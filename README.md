# Swarm Influence

Static redesign for Swarm Influence, prepared for GitHub Pages.

## Local preview

Open `index.html` in a browser, or run:

```sh
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
